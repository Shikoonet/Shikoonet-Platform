---
title: دریافت تست
description: سرویس آزمایشی یک‌بارمصرف
icon: Gift
---

دکمهٔ **دریافت تست** فقط وقتی دیده می‌شود که:

- کاربر قبلاً تست نگرفته باشد
- حالت تست در ربات روشن باشد
- پنل تست تنظیم شده باشد

ادمین می‌تواند از بخش عملیات گروهی، دریافت تست کاربران را ریست کند.
