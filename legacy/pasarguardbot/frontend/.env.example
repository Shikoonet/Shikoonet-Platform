# Browser-facing API base.
# Keep this relative in dev to use Vite proxy and avoid CORS issues.
VITE_API_BASE=/api/webapp

# Dev-only Vite proxy target for /api requests.
# Use the production backend when only the frontend is running locally.
VITE_API_PROXY_TARGET=https://your-bot-domain.example
