# Copy to .env.local (git-ignored) and fill in.
# .env.local holds real secrets and is NEVER committed.

# Local databases from docker-compose.yml
DATABASE_URL=postgres://shikoo:shikoo_local@localhost:5433/shikoo
LEGACY_MYSQL_URL=mysql://root:shikoo_local@localhost:3307/mirzabot

# The dedicated Telegram TEST bot. Never the production bot token.
TELEGRAM_BOT_TOKEN=
