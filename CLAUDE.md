# CLAUDE.md — قواعد کار روی این ریپو

> **از ۲۰۲۶-۰۸-۲۹ این فایل کامیت می‌شود.** پیش از آن در `.git/info/exclude` بود و فقط روی
> ماشین Sam وجود داشت. تصمیم Sam این بود که هر کسی — انسان یا ایجنت — که روی این ریپو کار
> می‌کند باید همین قواعد را ببیند. هم‌زمان ریپو **خصوصی** شد؛ تا آن روز عمومی بود و کسی
> حواسش نبود.
>
> پس این فایل را طوری بنویس که برای نفر بعدی معنا داشته باشد، نه فقط برای خودت.

`shikoo-platform` — ربات تجاری تلگرام، تایید پرداخت از روی SMS بانکی، و تحویل محصول، همه روی یک Postgres.
جانشین دو سیستم قبلی که در `legacy/` نگه داشته شده‌اند تا پوسته‌اندازی تمام شود.

---

## قواعد کاری (بالاترین اولویت)

### ۱. چیزی که خصوصی است، خصوصی می‌ماند

این فایل و `docs/` مشترک‌اند و کامیت می‌شوند. آنچه **هرگز** به گیت نمی‌رود:

- `.notes/` — کلید SSH سرور، توکن Coolify، لاگین پنل، اعتبارنامهٔ SMS
- `sim/.env.local` و هر `*.env` — توکن ربات، رمز پنل، `PANEL_SECRET_KEY`
- `legacy/mirzabot-php/db/` و هر `*.sql.gz` — پرداخت، آی‌دی تلگرام و کارت مشتری واقعی
- رسید بانکی واقعی (`e2e/fixtures/*.local.*`) — تصمیم Sam، ۲۰۲۶-۰۸-۲۴
- اسکرین‌شاتِ ریشهٔ ریپو — یک بار دوازده‌تا با `git add -A` رفت، بینشان چت شخصی

پلن‌های کاری هم بیرون می‌مانند؛ فقط **تغییرات کد و فیچرها** به بیرون می‌رود.

**ریپو خصوصی است و باید بماند.** ۲۰۲۶-۰۸-۲۹ معلوم شد یازده روز عمومی بوده، و
`docs/STATUS.md` تمام آن مدت می‌گفت «ریپوی خصوصی». یک جملهٔ نوشته‌شده شاهد نیست — همان
قاعدهٔ ۶، این بار دربارهٔ خودمان. اگر شک کردی بپرس: `gh repo view --json visibility`.

### ۲. مسیر رسیدنِ هر کار — قانون Sam، ۲۰۲۶-۰۸-۲۹

هیچ کاری از این مسیر بیرون نمی‌رود و هیچ پله‌ای پریده نمی‌شود:

```text
برنچ → PR (اولِ کار، به‌صورت draft) → کار → CI سبز
     → merge روی main — با کدام شرط، به «دروازهٔ دیپلوی» پایین بستگی دارد
     → Deploy Staging خودش شلیک می‌کند → shikoo-dev.chopon.uk
     → Promote Production — دستی، فقط وقتی بخواهیم نشانش بدهیم
```

#### دروازهٔ دیپلوی — و نویسندهٔ PR است که تعیین می‌کند کدام سؤال پرسیده شود

PR #25 مرج شد، CI سبز شد، و `Deploy Staging` در **یازده ثانیه** رد کرد.
`main` جلو رفت و استیجینگ نرفت — واگرایی‌ای که تنها نشانه‌اش یک ران قرمز است.

حالت زنده `owner-or-approved` است و **مالکان در خودِ ورک‌فلو نوشته شده‌اند**، نه
در یک متغیر مخفی: `.github/workflows/deploy-staging.yml` می‌گوید
`DEPLOY_OWNERS: Isusami,arshiajacki`. `deploy/approval-gate.sh` بر همین اساس دو
شاخه دارد:

- **PR نوشتهٔ یک مالک** → `ship_as_owner`. هیچ ریویویی لازم نیست و هیچ ریویویی
  هم جعل نمی‌شود؛ تنها شرط این است که **merge را هم یک مالک بزند**. لاگ آن را
  `policy=solo-owner` ثبت می‌کند و صریح می‌نویسد «کسی این را نخوانده».
- **PR نوشتهٔ هر کس دیگر** → `ship_as_approved` **به‌علاوهٔ** merge توسط مالک.
  یعنی: یک ریویوِ APPROVED از انسانی جز نویسنده، روی **آخرین کامیت** — کامیت
  تازه بعد از تایید، تایید را باطل می‌کند، و تاییدِ خودِ نویسنده و ریویوِ بات
  شمرده نمی‌شوند — و باز هم merge باید دستِ مالک باشد.

**استیجینگ دو مالک دارد، پروداکشن یکی.** این را با هم قاطی نکن:

| | مالکان | کجا نوشته شده |
| --- | --- | --- |
| Staging | `Isusami,arshiajacki` | `deploy-staging.yml` → `DEPLOY_OWNERS` |
| Production | فقط `Isusami` | `SOLO_DEPLOY_OWNER` در هر سه ورک‌فلوی پروداکشن |

`approval-gate.sh` اول `DEPLOY_OWNERS` را می‌خواند و `SOLO_DEPLOY_OWNER` فقط
شکل قدیمیِ تک‌نامی است که هنوز پذیرفته می‌شود. `prepare`، `promote` و `cutover`
هرکدام `SOLO_DEPLOY_OWNER` خودشان را دارند و `github.actor` را مستقیم با آن
می‌سنجند — پس اضافه‌شدن مالک دوم به استیجینگ، پروداکشن را گشاد نکرد.

> **این بند ۲۰۲۶-۰۸-۲۹ عوض شد و درسش از خودش بزرگ‌تر است.** تا آن روز این‌جا
> نوشته بود «`SOLO_DEPLOY_OWNER=Isusami`، پس `arshiajacki` هم ریویو می‌خواهد هم
> merge توسط Isusami». همان روز کامیت `559e7e3` مالک دوم را اضافه کرد و **این
> فایل هیچ‌وقت به‌روز نشد**. یعنی چند ساعت، قاعده‌ای که این‌جا نوشته شده بود با
> چیزی که سرور اجرا می‌کرد فرق داشت. پیش از اینکه به این بند تکیه کنی، از خودِ
> ورک‌فلو بپرس: `grep DEPLOY_OWNERS .github/workflows/deploy-staging.yml` —
> همان قاعدهٔ ۶.

هر ریویوِ `CHANGES_REQUESTED` بر همه‌چیز مقدم است.

تا `Deploy Staging` سبز نشده، **هیچ‌چیز به استیجینگ نرسیده** — merge را طوری
گزارش نکن که انگار رسیده.

**PR اول ساخته می‌شود، نه آخر.** به‌محض اینکه برنچ یک کامیت داشت، draft باز
می‌شود — تا کار در حین انجام دیده شود، نه اینکه تمام‌شده برسد.

#### سیاست Draft-first — ۲۰۲۶-۰۹-۰۲

دقایق Actions محدود است: این ریپو روی پلن Free خصوصی است با **۲۰۰۰ دقیقه در
ماه**، و آگوست ۲۰۲۶ اندازه‌گیری‌شده **۳۹۶۳** دقیقه مصرف کرد. یک PR در حالت
Ready حدود **۱۶ دقیقهٔ محاسبه‌شده** خرج می‌کند و یک draft حدود **۴**. کل صرفه‌جویی
همین است.

```
Draft PR
→ push‌های توسعه و چک‌های محلی
→ Sam اجازهٔ Ready می‌دهد
→ یک Full Gate روی همان head دقیق
→ Sam اجازهٔ merge می‌دهد
→ بررسی main
→ دیپلوی خودکار Staging
→ Production فقط با یک تایید دستی جدا
```

قواعدی که هیچ ایجنتی نمی‌شکند:

- هر PR **draft** ساخته می‌شود؛ کامیت‌های توسعه روی همان draft می‌مانند.
- Draft/Ready را **مکرر جابه‌جا نکن**. هر بار Ready یک گیت کامل خرج می‌کند.
- **فقط یک‌بار** Ready بزن، و فقط وقتی کار تمام است، اعتبارسنجی محلی سبز است،
  برنچ تمیز است، کامیت دیگری در راه نیست، و **Sam صریحاً اجازه داده**.
- **بدون اجازهٔ صریح Sam هیچ‌وقت Ready نزن** و **هیچ‌وقت merge نکن** — حتی وقتی
  همه‌چیز سبز است. سبز بودن پیش‌شرط است، نه اجازه.
- Ready راهی برای «گرفتن فیدبک» نیست؛ گیت گران را شلیک می‌کند.
- کامیت خالی یا تغییر بی‌معنا فقط برای تریگر CI ممنوع است، مگر Sam بخواهد.
- **هر کامیتی بعد از Full Gate، یک Full Gate تازه روی head جدید لازم دارد** —
  و باید همان‌جا گزارش شود.
- تا `Required Quality Gate` روی **همان head نهایی** سبز نشده، merge نکن.
- skip فقط وقتی قابل قبول است که `tools/ci-plan.sh` اعلامش کرده باشد و
  `Required Quality Gate` تاییدش کند. verifierی که skip شده، غایب است، یا بدون
  هیچ assertion سبز می‌شود، **مدرک نیست**.
- برای صرفه‌جویی در دقیقه‌ها، هیچ تست لازم، floor، coverage، چک اسکیما یا چک
  امنیتی را ضعیف نکن.
- کار کوچک مستندات یا تمیزکاری را با تغییر واقعی بعدی همراه کن، نه در PR جدا.
- تعارض که پیش آمد، **دروازه‌های ایمنی و اجازهٔ صریح Sam** مقدم‌اند، نه سرعت.

متن کاملِ این سیاست — با دلیل هر بند — در
`.claude/skills/agent-ground-rules/SKILL.md` است و همان نسخهٔ مرجع برای همهٔ
ایجنت‌هاست.

**push روی برنچ خودی روتین است** و به دست هیچ‌کس نمی‌رسد.
**merge روی `main` روتین نیست** — همان پله‌ای است که به استیجینگ می‌رسد و راهِ
مشتری از آن‌جا شروع می‌شود.

#### روی سرور، اول بپرس این کدام محیط است — بعد دست بزن

**اشتباه من، ۲۰۲۶-۰۹-۰۱، و این‌جاست چون بندِ بالا را دور زد.** `.notes/deploy.env`
نوشته بود `IR_APP_DASHBOARD='huneuqvzyw0cjd4u0f7s37cf'`. خواندمش، «داشبورد» فهمیدم،
دو متغیر رویش گذاشتم و **دیپلوی زدم**. آن uuid **پروداکشن** بود — `shikoo.chopon.uk`.
«Promote Production هیچ‌وقت کار تو نیست» با یک خط از یک فایل یادداشت دور خورد.

چیزی نشکست، و دلیلش را باید نوشت چون کارِ من نبود: ایمیج تازه دروازهٔ اسکیما را روی
دیتابیسی پنج مهاجرت عقب اجرا کرد، بالا نیامد، و Coolify به کانتینر قبلی برگشت.
**دروازه تمام کارش را کرد.** اگر آن دیتابیس هم‌سطح بود، پروداکشن بی‌خبر عوض می‌شد.

همان روز همین اشتباه یک بار دیگر هم شد: `IR_PG_CONTAINER` کانتینر Postgres یک استکِ
قدیمی‌تر را نام می‌برد، و روی آن سرور **دو** Postgres هست که **هر دو** دیتابیسی به اسم
`shikoo` با همان جدول‌ها دارند. نیم ساعت اشتباهی را شمردم و نزدیک بود گزارش بدهم
«مهاجرت‌ها اجرا نشده‌اند». هیچ کوئری‌ای خطا نداد — تنها نشانه این بود که
`schema_migrations` آن یکی روی `0037` مانده بود.

**قاعده: نام محیط از یادداشت خوانده نمی‌شود، از خودِ سرور پرسیده می‌شود.** پیش از هر
PATCH، هر DELETE و هر دیپلوی، این دو را بزن:

```bash
# کدام اپ، کدام دامنه — هرچه `*-dev` و `shikoo-dev.chopon.uk` است استیجینگ، باقی پروداکشن
curl -H "Authorization: Bearer $IR_COOLIFY_TOKEN" http://localhost:8000/api/v1/applications \
  | python3 -c 'import sys,json;[print(a["uuid"],a.get("name"),a.get("fqdn")) for a in json.load(sys.stdin)]'

# کدام دیتابیس را خودِ اپ باز می‌کند — نه آنکه یادداشت می‌گوید
docker inspect -f '{{range .Config.Env}}{{println .}}{{end}}' <container> | grep DATABASE_URL
```

**اگر خروجی این دو را ندیده‌ای، هنوز نمی‌دانی کجایی.**

درسش از خودش بزرگ‌تر است و همان قاعدهٔ ۶ است، این بار دربارهٔ زیرساخت: یک uuid هیچ‌چیز
دربارهٔ محیط نمی‌گوید، و دو چیزِ هم‌نام هیچ خطایی نمی‌دهند. یک فایل یادداشت شاهد نیست.

#### گزارش پنج‌قسمتی — دروازهٔ merge

پیش از merge (نه پیش از push برنچ) — **به ترتیب و دقیق** به Sam:

1. چه کامیت‌هایی ساخته شد (SHA + پیام)
2. چه فایل‌هایی عوض شد — `git diff --stat`
3. چه چیزی در ارسال **نیست** چون خصوصی است
4. چه تست‌هایی سبز شد، **با تاریخ**، و روی کدام محیط تایید شد
5. متن PR

**بدون تایید صریح Sam، merge نکن.** و `Promote Production` هیچ‌وقت خودکار نیست
و هیچ‌وقت کار تو نیست.

> **این جابه‌جایی را عمداً کردم و باید ببینی‌اش.** قبلاً این‌جا نوشته بود «بدون
> تایید صریح Sam، **push** نکن». ولی «PR اول ساخته شود» بدون push ممکن نیست، و
> گزارشِ «کدام تست‌ها سبز شد» وقتی کار هنوز شروع نشده معنا ندارد. پس دروازه از
> push به **merge** منتقل شد — پله‌ای که واقعاً برگشت‌ناپذیر است. اگر منظورت این
> نبود، بگو تا برش گردانم.

**هیچ‌وقت روی `dev` کامیت نکن.** آن برنچ آزمایشِ رهاشدهٔ ۲۰۲۶-۰۸-۲۶ است، ۱۳۱
کامیت عقب و قرمز. `shikoo-dev` یک **محیط** است که از `main` تغذیه می‌شود.

### ۳. قبل از شروع هر کار، همگام شو

```bash
git fetch --all --tags --prune
git log --oneline HEAD..@{u}
git status --short
```

اگر کامیت جدیدی آمده: بخوانش، خلاصه کن، و چک کن `migrations/` یا `pnpm-lock.yaml` عوض شده یا نه.

### ۴. عملیات فایل انبوه روی ویندوز — کپی کن، تایید کن، بعد حذف

**درس ۲۰۲۶-۰۸-۱۲، با هزینهٔ واقعی.** `Move-Item`/`mv` روی پوشه‌های بزرگ این پروژه **نیمه‌کاره تمام می‌شود و در عین حال خطا گزارش می‌کند**. یک بار `.git` کامل و همهٔ فایل‌های ریشهٔ یک ریپو این‌طور جابه‌جا شدند و بعد با یک `Remove-Item -Recurse -Force` روی مقصدِ ظاهراً ناموفق پاک شدند. بازیابی فقط چون ریموت گیت‌هاب وجود داشت ممکن شد.

- **هرگز `Move-Item`/`mv` روی پوشهٔ بزرگ نزن** — از `robocopy <src> <dst> /E` استفاده کن.
- **قبل از هر `Remove-Item -Recurse`، داخل مقصد را بشمار.** «عملیات خطا داد» یعنی «شاید نصفش انجام شده»، نه «هیچ کاری نشده».
- **اول تایید، بعد حذف منبع.** معیار تایید برای یک ریپو: `git status --short` خالی و hash درخت با ریموت یکی.
- **cwd هیچ شلی نباید داخل پوشه‌ای باشد که جابه‌جا می‌شود** — قفل ویندوز از همان‌جا می‌آید.

### ۵. تست زمان‌دار: ساعت را pin کن، تاریخ را hardcode نکن

هر تستی که کدِ زیرش `Date.now()` واقعی را می‌خواند و انتظارش به تاریخ ثابتی گره خورده، **بمب ساعتی** است. نمونهٔ واقعی: تستی که `NOW_MS` را روی یک تاریخ میخ کرده بود و ساعت ۰۰:۰۰ به وقت تهران ترکید. دومی: `purchase-type-dev.test.ts` که از روز نوشته‌شدنش قرمز بود و کسی ندید.

درستش: `vi.spyOn(Date, 'now').mockReturnValue(NOW_MS)` در `beforeEach` و `vi.restoreAllMocks()` در `afterEach`.

### ۶. تستی که فقط با خودش می‌خواند، هیچ چیز را ثابت نمی‌کند

**درس ۲۰۲۶-۰۸-۱۲، با یک باگ زندهٔ پروداکشن.** تابع «امروز به وقت تهران» ۷ ساعت جابه‌جا بود و
تست‌ها سبز بودند — چون فقط بررسی می‌کردند که تابع با **خودش** سازگار است
(`historyRangeBounds('today')` برابر `tehranDayFromUtc`). حتی کامنت‌های تست با ثابت‌هایشان تناقض
داشت.

هر جا حقیقتِ بیرونی وجود دارد، تست باید با **آن** بسنجد، نه با کد زیر آزمون:
منطقهٔ زمانی → `Intl` روی `Asia/Tehran` · شمارهٔ کارت → رقم کنترلی Luhn ·
اسکیما → خودِ Postgres · پول → جمع سمت مبدأ.

### ۷. چک باید دست‌کم به سخت‌گیریِ کدی باشد که چک می‌کند

پیش‌پروازِ مهاجرت گفت «همهٔ مقادیر نگاشت شده‌اند» و بعد مهاجرت روی مقدار `active` (حروف کوچک)
ایستاد. علت: `GROUP BY` در MySQL به بزرگی حروف حساس نیست و `Active` و `active` را یکی شمرده بود.
هر بررسی مقدارِ یکتا روی MySQL باید `COLLATE utf8mb4_bin` داشته باشد.

### ۸. تست‌های دیتابیس سریالی اجرا می‌شوند — و `-r` تنها این را نمی‌دهد

`pnpm test` ریشه **`pnpm -r --workspace-concurrency=1 test`** است. چند پکیج روی یک Postgres
تست می‌گیرند و بینشان `TRUNCATE` می‌زنند؛ موازی یعنی همدیگر را پاک می‌کنند.

**تصحیح ۲۰۲۶-۰۸-۱۲:** این‌جا قبلاً نوشته بود «`pnpm -r test` است نه `--parallel`، پس سریالی
است». **غلط بود.** نبودِ `--parallel` فقط ترتیب توپولوژیک را نگه می‌دارد؛ پکیج‌هایی که به هم
وابسته نیستند باز هم هم‌زمان اجرا می‌شوند. `apps/bot` و `apps/dashboard-worker` خواهر‌اند، پس
با هم بالا می‌آمدند — و `dashboard-worker` روی همان Postgres `payment_cards`،
`financial_accounts` و `payment_claims` را truncate می‌کند. تا وقتی تست‌های ربات فقط `users`
و `orders` و کاتالوگ را می‌خواندند کسی ندید؛ روزی که ربات به `payment_cards` وابسته شد،
تست‌ها تنها سبز بودند و در اجرای کامل قرمز. تنها چیزی که سریالی بودن را واقعاً تضمین می‌کند
`--workspace-concurrency=1` است.

درسش از خود باگ بزرگ‌تر است: **یک کامنت که می‌گوید چرا چیزی امن است، خودش شاهد نیست.** این
جمله ماه‌ها این‌جا بود و کسی از بیرون نسنجیدش — دقیقاً همان الگوی قاعدهٔ ۶.

`typecheck` و `lint` موازی می‌مانند — حالت مشترکی ندارند.

### ۹. `LIMIT` در ساب‌کوئری سقفِ کل کار نیست

**درس ۲۰۲۶-۰۸-۱۹.** سه جا ادعا شده بود «هر بار حداکثر N ردیف برمی‌داریم»: `webhook_deliveries`،
`bot_notifications` و `broadcast_recipients`. دوتاشان دروغ بود.

`UPDATE … WHERE id IN (SELECT id … LIMIT 2)` سقف نیست. اگر پلنر ساب‌کوئری را سمتِ **داخلیِ**
nested loop بگذارد، به ازای هر ردیف بیرونی دوباره اجرایش می‌کند و `LIMIT` هر اجرا را می‌بندد،
نه دسته را. برای limit=۲ روی جدول پر، ۹ ردیف برداشته شد.

`(a,b) IN (SELECT … LIMIT n)` هم درمان نیست — همان چیز با شکل دیگر. درمان فنسِ مستندِ خودِ
Postgres است:

```sql
WITH due AS MATERIALIZED (SELECT id FROM … ORDER BY … LIMIT ?1 FOR UPDATE SKIP LOCKED)
UPDATE … WHERE id IN (SELECT id FROM due)
```

**و اثباتش فقط روی جدول پر معنی دارد.** همین باگ اول با یک probe دستی روی جدول تقریباً خالی
«سالم» اعلام شد؛ چند دقیقه بعد همان دستور روی همان دیتا ۸ برگرداند. جدولی که سه ردیف دارد
هیچ‌وقت nested loop نمی‌گیرد — یعنی تستی که جدول را پر نمی‌کند، دربارهٔ این باگ **ساکت** است،
نه سبز.

---

## ابزارها — همیشه فعال

### ۰. این پروژه را از `shikoo-platform/` باز کن

نه از پوشهٔ والد `ShikooNet/`. از والد نه این فایل لود می‌شود و نه هوک‌های graphify شلیک می‌کنند — چون `graphify-out/` را نسبت به cwd می‌گردند.

### graphify — قبل از grep، نه بعدش

قواعد کامل در بخش `## graphify` انتهای همین فایل است (خودِ ابزار می‌نویسدش، دستی ویرایشش نکن). سه نکتهٔ اضافه:

- **strict فعال است:** اولین Read خام هر سشن بلاک می‌شود تا یک `graphify query` اجرا شود. عمدی است، دورش نزن. فقط اگر گراف واقعاً کهنه بود: `GRAPHIFY_HOOK_STRICT=0`.
- **همین قاعده را در پرامپت هر subagent تکرار کن** — هوک برای ساب‌ایجنت‌ها شلیک نمی‌کند و بیشترین هدررفت توکن همان‌جاست.
- گراف فقط کد است (`--code-only`) و `legacy/` را هم پوشش می‌دهد، چون حین بازنویسی مرجع ماست.

### ponytail — سطح `full`

سادگی پیش‌فرض است. اما در این ریپو **هرگز** ساده نمی‌شوند:
اعتبارسنجی ورودی روی مرز اعتماد · هر مسیر پول · احراز هویت و RBAC · و کل بخش «قواعدی که نباید بشکنند».

### Playwright MCP — باگ UI را ببین، حدس نزن

هر تغییر داشبورد **قبل** از fix با Playwright روی محیط شبیه‌سازی بازتولید و **بعد** از fix با همان ابزار تایید شود. استدلال از روی کد به‌تنهایی کافی نیست.

### محیط شبیه‌سازی — `sim/`

**امروز:** `pnpm sim:up` فقط دو دیتابیس بالا می‌آورد — Postgres روی `5433` و MySQL روی `3307`
با دامپ پروداکشن. کل ۸۶۸ تست روی همین اجرا می‌شوند و کاملاً آفلاین‌اند.

**هنوز ساخته نشده:** تلگرام قلابی، پنل provisioning قلابی، و سناریوهای Playwright.
اولی و دومی وقتی معنا دارند که ربات وجود داشته باشد (فاز ۴). سومی الان هم می‌شود ساخت —
داشبورد با `TEST_ACCESS_USER` بدون JWT بالا می‌آید.

قبل از هر ریلیز یک مجموعهٔ دودی روی ربات تست واقعی اجرا می‌شود؛ توکنش در `sim/.env.local` است که
git-ignored است.

**هیچ‌وقت روی ربات واقعی یا دیتای واقعی مشتری تست نکن.**

### ایجنت‌ها و اسکیل‌ها

`.claude/agents/` یازده ایجنت دارد. اسکیل‌های دامنه‌ای در `.claude/skills/` انتخابی نیستند — قبل از دست‌زدن به کد مربوطه الزامی‌اند. از بقیهٔ پلاگین‌ها هر وقت مناسب بود آزادانه استفاده کن (`context7` برای مستندات کتابخانه، `code-review` و `security-review` قبل از PR، `frontend-design` برای UI).

---

## قواعدی که نباید بشکنند

- **auto-verify فقط برای جفت ایزولهٔ ۱↔۱**: حساب دقیق، مبلغ دقیق بدون تلورانس، `|bank_timestamp − paid_clicked_at| ≤ 300000ms`. هیچ‌وقت auto-reject نکن، هیچ‌وقت رسید را خودکار fake نزن.
- **یک تراکنش بانکی نمی‌تواند چند claim را verify کند، و یک claim بیش از یک بار settle نمی‌شود.** این را **ایندکس partial unique در Postgres** نگه می‌دارد، نه کد. هرگز این تضمین را به لایهٔ اپلیکیشن منتقل نکن.
- **پول همه‌جا IRR صحیح است.** تومان فقط در لبهٔ ربات وجود دارد و با یک helper تست‌شده تبدیل می‌شود (`amountToman * 10`).
- **اسکیما فقط با مهاجرت نسخه‌دار عوض می‌شود.** هیچ DDL در زمان اجرا — ربات قدیمی روی _هر_ آپدیت تلگرام `SHOW COLUMNS` می‌زد؛ این برنگردد.
- `audit_logs` فقط append است — نه update، نه delete.
- OTP هرگز ذخیره، رندر یا لاگ نمی‌شود. apiKey، بات‌توکن، کلید HMAC و بدنهٔ خام SMS هرگز لاگ نمی‌شوند.
- **اپ اندروید sms-relay دست نمی‌خورد** و قرارداد `POST /api/v1/sms` منجمد است — فقط URL در تنظیماتش عوض می‌شود.
- **سرور ربات فعلی فقط-خواندنی است.** تا پایان پوسته‌اندازی هیچ تغییری روی آن انجام نمی‌شود؛ رفع باگ‌هایش کار ادمین است. سیاههٔ آن باگ‌ها `BUGS-FOR-ADMIN.md` بود که ۲۰۲۶-۰۸-۲۹ حذف شد؛ هر یافته‌ای که به آن ارجاع می‌دهد در [`docs/STATUS.md`](docs/STATUS.md) ثبت است.

---

## معماری در یک نگاه

```text
یک سرور (Ubuntu 24.04)
├── Postgres 16              ← تنها منبع حقیقت
├── bot        long polling  ← بدون پورت عمومی، بدون گواهی SSL
├── ingest                   ← تنها سطح عمومی: POST /api/v1/sms
├── dashboard + SPA          ← پشت Cloudflare Access
└── worker (cron)            ← انقضای claim، تطبیق، تحویل
        ▲
        │ Cloudflare Tunnel — بدون پورت باز، IP مخفی، SSL خودکار
```

مدل محصول عمومی است: `product → provisioning_kind` با یک آداپتور برای هر نوع
(`marzban`, `marzneshin`, `hiddify`, `xui`, `wireguard`, `ai_account`, `spotify`, `manual`).
افزودن محصول جدید یعنی یک آداپتور جدید — بدون دست‌زدن به سفارش، کیف پول، فاکتور یا تایید پرداخت.

## `packages/db` — درزی که کل مهاجرت روی آن ایستاده

آداپتور Postgres که اینترفیس D1 را پیاده می‌کند. **همهٔ کوئری‌ها از این‌جا رد می‌شوند.** اگر
رفتاری بین SQLite و Postgres فرق کرد، جایش این‌جاست نه ۳۷۰ دستور SQL:

- `?N` و `?` هر دو به `$n` ترجمه می‌شوند؛ قاطی‌شان در یک دستور **رد** می‌شود، نه حدس
- `INSERT OR IGNORE` → `ON CONFLICT DO NOTHING` — که فقط تعارض یکتایی را می‌خورد، نه `CHECK`
- شکاف پارامتر بسته می‌شود (SQLite پارامتر بی‌استفاده را نادیده می‌گیرد، Postgres نمی‌تواند)
- `int8` و `numeric` عدد می‌شوند، با بررسی از دست نرفتن دقت
- `batch()` تراکنش واقعی است
- خطای درایور **خودِ دستور را همراه دارد**

جدول کامل «علامت ← علت ← درمان» در [`docs/STATUS.md`](docs/STATUS.md) است.

## `legacy/` — مرجع، نه کد زنده

- `legacy/hub-cloudflare` — نسخهٔ Cloudflare. **دیگر از آن کد نمی‌آید؛ همه‌چیز منتقل شده.** فقط
  دو چیز هنوز به آن وابسته است: `hub-sql.test.ts` که هر دستور SQL آن را روی اسکیمای جدید
  `PREPARE` می‌کند، و اکسپورت D1 در `.production-backups/` که ورودی مهاجرت است.
- `legacy/mirzabot-php` — ربات PHP زنده. مرجع **رفتار** است، نه الگوی کد.
- `legacy/faoxima` و `legacy/pasarguardbot` — دو ربات دیگر، برای مقایسه.

هر چهار پوشه از ۲۰۲۶-۰۸-۲۹ **در گیت‌اند**. تا آن روز ignore بودند و هر کدام `.git` خودشان
را داشتند؛ آن تاریخچه‌ها به `.notes/legacy-git/` منتقل شدند — با تایید بایت‌به‌بایت و HEAD —
چون گیت ریپوی تودرتو را به‌صورت gitlink ثبت می‌کند و روی گیت‌هاب پوشهٔ خالیِ کلیک‌ناپذیر
می‌شود. ریموت و HEAD هرکدام در `.notes/legacy-git/README.md` است.

`legacy/hub-cloudflare` در فاز ۵ حذف می‌شود؛ سه‌تای دیگر تا وقتی مرجعِ رفتارند می‌مانند.

---

## وضعیت و پلن

- **وضعیت لحظه‌ای، دستورهای اجرا، و راهنمای عیب‌یابی: [`docs/STATUS.md`](docs/STATUS.md)** —
  اول این را بخوان.
- طراحی اسکیما و نگاشت جدول‌ها: [`docs/schema-design.md`](docs/schema-design.md)
- پلن کامل: `~/.claude/plans/shikoo-platform.md` — محلی است و کامیت نمی‌شود. اگر روی ماشین
  دیگری کار می‌کنی، `docs/STATUS.md` جانشین کاملش است.

فازها: ۱ پایه ✅ · ۲ دیتا ✅ · ۳ انتقال Hub ✅ · **۴ هستهٔ ربات جدید ← این‌جاییم** ·
۵ مهاجرت فیچر به فیچر و بازنشستگی PHP.

**کجای فاز ۴ (۲۰۲۶-۰۸-۲۲): پاریتیِ باز ندارد.** هسته ساخته شده، هر ۲۴ یافتهٔ بازبینی فنی
بیرونی بسته است، و فهرست «پاریتی نساخته» امروز خالی شد. رفته‌ها: گزارش شبانه (۰۸-۱۹) ·
`on_hold` و سه سوییچ از شش‌تا · ضد-اسپم · تنظیم گروهی قیمت (۰۸-۲۰) · اثبات `READ_ONLY`
در مرورگر (۰۸-۲۲).

سه قلم آخر **ساخته نمی‌شوند**، و هر سه با پرسیدن از خودِ دامپ بسته شدند نه با خواندن فهرست:
تیکت و دپارتمان (خاموش در پروداکشن؛ ۷ تیکت، هر هفت `Unseen` — تصمیم Sam ۰۸-۲۲) ·
`categoryhelp` (روشن، ولی ۴ دسته × ۱ مقاله با همان متن) · `status_keyboard_config` (روشن در
سطح فروشگاه، ولی هر پنج پنل `offconfig`، پس هرگز شلیک نکرده). جزئیات و شواهد در
`docs/STATUS.md` › «۶. پاریتی نساخته».

**درسش را جای دیگر هم به کار ببر: سوییچی که `1` است ثابت نمی‌کند فیچری زنده است.** دوتا از
آن سه‌تا `1` بودند و هیچ‌کدام کار نمی‌کردند. برای هر «شکاف» اول از داده بپرس که آیا اصلاً
شلیک می‌کند.

**گردونهٔ شانس ساخته نمی‌شود** — تصمیم Sam، ۲۰۲۶-۰۸-۲۰: «ما گردونه شانس نداریم». روی PHP
فعال بود (۱٬۶۷۷ چرخش، آخرینش ۰۷-۲۸)، پس اگر کسی «شکاف» دید، شکاف نیست — تصمیم است. سوییچ
`Dice` هم جزئی از همین است و با آن می‌رود.

**سرور Coolify روی `164.132.198.184` جعبهٔ تمرین است، نه پروداکشن** — تصمیم صریح Sam،
۲۰۲۶-۰۸-۱۹: «حکم تکرار و تمرین و دیباگ داره برامون، واقعی نیست اصلا». دیتایش واقعی نیست و
uptimeش نگرانی مشتری نیست. کار را با «آیا نرم‌افزار کارِ درست را می‌کند» بسنج، نه با «آیا
دیپلوی امن است». این هیچ‌چیز از «هرگز روی سیستم زنده تست نکن» کم نمی‌کند — ربات PHP، پنل
مشتری و دیتای واقعی فقط-خواندنی می‌مانند.

## graphify

This project has a knowledge graph at graphify-out/ with god nodes, community structure, and cross-file relationships.

Rules:

- For codebase questions, first run `graphify query "<question>"` when graphify-out/graph.json exists. Use `graphify path "<A>" "<B>"` for relationships and `graphify explain "<concept>"` for focused concepts. These return a scoped subgraph, usually much smaller than GRAPH_REPORT.md or raw grep output.
- If graphify-out/wiki/index.md exists, use it for broad navigation instead of raw source browsing.
- Read graphify-out/GRAPH_REPORT.md only for broad architecture review or when query/path/explain do not surface enough context.
- After modifying code, run `graphify update .` to keep the graph current (AST-only, no API cost).
